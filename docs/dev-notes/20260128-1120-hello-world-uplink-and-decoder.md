2026-01-28 11:20 – Added a one-time \"HELLO WORLD\" uplink on join (FPort 2) and extended the JavaScript decoder to treat FPort 2 as a plain text message (`data.message`), keeping the existing batched temperature decoder on FPort 1 unchanged for normal operation.

